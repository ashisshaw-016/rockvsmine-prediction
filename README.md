# rockvsmine-prediction
Trained a Machine Learning model to distinguish between rocks and underwater mines — because real-world trial and error isn’t exactly an option 😄

This project uses Python and Logistic Regression to classify sonar signals into two categories: Rock (R) or Mine (M). The goal is to transform raw sonar data into accurate, data-driven predictions
